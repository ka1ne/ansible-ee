# ee/requirements.txt — Python packages baked into the Execution Environment.

# ── WinRM transport ─────────────────────────────────────────────────────────
# NTLM covers workgroup and local-account targets; CredSSP is needed for
# second-hop scenarios such as reaching a file share from the Windows host.
pywinrm==0.4.3
requests-ntlm==1.3.0
requests-credssp==2.0.0

# Kerberos, for domain-joined targets. Needs the krb5 system packages already
# declared in bindep.txt. Left commented until a domain-joined target is in
# scope, since it adds build time for everyone else.
# pykerberos==1.2.4
# requests-kerberos==0.14.0

# ── Filters and parsing used by the playbooks ───────────────────────────────
jmespath==1.1.0
xmltodict==0.13.0
