---
all:
  children:
    windows:
      hosts:
        stg-win-01.ka1ne.dev:
      vars:
        ansible_connection: winrm
        ansible_winrm_transport: credssp
        ansible_winrm_server_cert_validation: ignore
        ansible_port: 5986

    linux:
      hosts:
        stg-linux-01.ka1ne.dev:
      vars:
        ansible_connection: ssh
        ansible_port: 22

  vars:
    env: staging
