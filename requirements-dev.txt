# Tooling for working on this repository. Not baked into the EE image — the
# image's own dependencies live in ee/requirements.txt.
#
#   make venv    creates .venv and installs this
#   CI           pip install -r requirements-dev.txt

# Ansible itself, so ansible-lint can resolve modules and playbooks can be
# syntax-checked outside a container.
ansible-core>=2.15,<2.21

# EE build toolchain
ansible-builder>=3.1,<4

# Linting
ansible-lint>=26.8.0
yamllint>=1.35,<2

# Local EE smoke-testing
ansible-navigator>=24.0

# Role and playbook testing
molecule>=24.0
molecule-plugins[docker]>=23.5,<24

# Runtime packages the playbooks import when run outside the EE.
-r ee/requirements.txt
