# Dev / test environment for Ansible EE
# Install via: make venv  (or pip install -r requirements-dev.txt)

# EE build toolchain
ansible-builder>=3.1,<4

# Linting
ansible-lint>=25.0,<26
yamllint>=1.35,<2

# Navigator (local EE smoke-testing)
ansible-navigator>=24.0,<25

# Role / playbook testing
molecule>=24.0,<25
molecule-plugins[docker]>=23.5,<24

# Unit / integration testing helpers
pytest>=6.0,<8
pytest-ansible>=4.1,<5

# Runtime deps (keep in sync with requirements.txt)
-r requirements.txt
